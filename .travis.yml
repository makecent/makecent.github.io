language: ruby
cache: bundler
gemfile: docs/Gemfile
script:
  - ALGOLIA_API_KEY a4a827f5953f2c8caf25d9b41d856a99 bundle exec jekyll algolia --source docs --destination docs/_site --config docs/_config.yml
branches:
  only:
    # Change this to gh-pages if you're deploying using the gh-pages branch
    - gh-pages
rvm:
 - 2.4
