---
title: "Useful Console Commands"
---

1. list number of files ``ls regex | wc -l``

  ```shell
  ls . | wc -l
  ls /foo/*.imgs | wc -l
  ```
  
2. check disk size of folder ``du -hs``

  ```shell
  du -hs ./datasets
  ```
